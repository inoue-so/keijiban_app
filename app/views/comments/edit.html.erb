
<% if @comment.errors.any? %>
   <% @comment.errors.full_messages.each do |e| %>
     <%= e %>
   <% end %>
  <% end %>
  <%= form_for [@board, @comment] do |f| %>
   <%= f.text_area :content %>
   <%= f.submit %>
 <% end %> 
